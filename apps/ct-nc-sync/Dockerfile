# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build ct-nc-sync`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t ct-nc-sync`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system ct-nc-sync && \
          adduser --system -G ct-nc-sync ct-nc-sync

COPY dist/apps/ct-nc-sync ct-nc-sync
RUN chown -R ct-nc-sync:ct-nc-sync .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix ct-nc-sync --omit=dev -f install

CMD [ "node", "ct-nc-sync" ]
